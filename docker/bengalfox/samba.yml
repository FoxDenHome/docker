version: "2.1"
services:
  samba:
    image: ghcr.io/doridian/docker-samba/samba:latest
    networks:
      vlan4:
        ipv4_address: 192.168.4.20
    volumes:
      - samba_config:/var/lib/samba
      - /mnt/zhdd/nas:/mnt/nas
      - /mnt/zssd/nas/torrent:/mnt/nas/torrent
      - /mnt/zhdd/nashome:/home

volumes:
  samba_config:
    external: true
