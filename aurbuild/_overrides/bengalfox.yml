services:
  aurbuild:
    volumes:
      - /mnt/zssd/docker/aurbuild/cache:/aur/cache
      - /mnt/zssd/docker/aurbuild/repo:/aur/repo
      - /mnt/zssd/docker/aurbuild/repo_new:/aur/repo_new
  archzfs:
    volumes:
      - /mnt/zssd/docker/archzfs/tmp:/tmp
      - /mnt/zssd/docker/archzfs/repo:/repo
