services:
  aurbuild:
    build: aurbuild
    restart: always
    environment:
      GPG_KEY_ID: "${GPG_KEY_ID}"
      GPG_KEY_PATH: /gpg/foxdenaur.asc
    volumes:
      - cache:/cache
      - gpg:/gpg:ro

volumes:
  cache: {}
  gpg: {}
