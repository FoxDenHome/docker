services:
  aurbuild:
    build: aurbuild
    restart: always
    init: true
    userns_mode: host
    environment:
      GPG_KEY_ID: "723AB072D36DF76677DA5ACF41ADC5FF876838A8"
    volumes:
      - /etc/pacman.d/mirrorlist:/etc/pacman.d/mirrorlist:ro
      - /etc/makepkg.conf:/etc/makepkg.conf:ro
      - /run/pcscd/pcscd.comm:/run/pcscd/pcscd.comm
