_server_contact = { "doridian@foxden.network" }

contact_info = {
  abuse = _server_contact;
  admin = _server_contact;
  feedback = _server_contact;
  sales = _server_contact;
  security = _server_contact;
  support = _server_contact;
}
